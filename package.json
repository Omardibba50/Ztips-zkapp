{
  "name": "ztips-zkapp",
  "version": "1.0.0",
  "description": "ZTips - Decentralized Information Exchange Platform",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install && cd ../contracts && npm install",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "test": "cd contracts && npm test",
    "deploy": "cd contracts && npm run deploy"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
